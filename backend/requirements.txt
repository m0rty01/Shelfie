# Shelfie backend dependencies
# Install with: pip install -r requirements.txt

# Web framework (required by spec)
django==4.2.23
djangorestframework==3.15.2
django-cors-headers==4.7.0

# Server
gunicorn==23.0.0

# Computer vision — local spine detection
opencv-python-headless==4.10.0.84
numpy==2.2.6
Pillow==11.3.0

# Local pretrained spine detector (Tesseract LSTM text region detection)
# Requires: sudo apt install tesseract-ocr
pytesseract==0.3.13

# Hosted vision-language model (Gemini Flash)
google-genai==1.26.0

# Fuzzy catalog matching
rapidfuzz==3.13.0

# Env config
python-dotenv==1.1.1
